AM_TESTS_ENVIRONMENT = \
  PYTHONPATH=$(top_srcdir):$(PYTHONPATH)

TEST_LIBRARY = coretest.py

REGTESTS = \
  attach_detach.py \
  block_data.py \
  moves.py

EXTRADIST = $(REGTESTS) $(TEST_LIBRARY)
TESTS = $(REGTESTS)
